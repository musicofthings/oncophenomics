// postcss.config.js — Tailwind v4
// @tailwindcss/postcss replaces the old 'tailwindcss' plugin.
// autoprefixer is now built into Tailwind v4 — removed.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
